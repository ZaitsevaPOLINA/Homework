#include <iostream>
#include <string>

int main()
{
  void dfs(int v){
  used[v]=true;
  for(size_t i=0; i<g[j].size(); ++i) {
				int to = g[i][j];
        if (!used[to]) dfs(to);
        ans.push_back(to);}
        void top_sort(){
        for(int i=0; i<v; i++)
        if (!used[i]) dfs(i);
        reverge(ans.begin(),ans.end());}
        return 0;}
        
        
