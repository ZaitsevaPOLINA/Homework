#include <iostream> 
#include <string> 
using namespace std; 

void countingsort(int n,int A[n],int k) 
{ 
for ( int i = 0; i<k; i++) 
C[i] = 0; 
for (int i = 0; i< n; i++) 
C[A[i]]++; 
for(int j=0; j<k; j++) 
for(int i=0; i<c[j]; i++) 
cout « j; 
} 

int main() 
{ 
int k; 
cin » k; 
int n; 
cin » n; 
int A[n]; 
for (int i = 0; i<n; i++) 
cin » A[i]; 
countingsort( A[n], k, n); 
return 0; 
}
